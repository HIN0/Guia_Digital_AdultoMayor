# Variables para docker-compose.yml (Postgres local).
# Copiar a ".env" en la raíz del repo si quieres cambiar el password por defecto.
# Las credenciales reales del backend van en backend/.env (ver backend/.env.example).
POSTGRES_PASSWORD=54321
