{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{try{const j=JSON.parse(d);const f=(j.tool_input&&j.tool_input.file_path)||'';process.exit(/\\.(ts|tsx)$/.test(f)?0:1);}catch{process.exit(1);}})\" && (cd frontend && npm run lint 2>&1 | head -40) || true",
            "timeout": 30,
            "statusMessage": "Verificando ESLint..."
          }
        ]
      }
    ]
  }
}
